# Override local. Todos los puertos se enlazan solo a loopback.
services:
  db:
    ports:
      - "127.0.0.1:5432:5432"

  backend:
    ports:
      - "127.0.0.1:8001:8000"

  caddy:
    ports:
      - "127.0.0.1:8080:8080"
